# Pinned developer / CI dependencies for Hermes Guardian.
#
# The Guardian plugin itself has no runtime dependencies (pure standard-library
# Python); optional integrations are declared as extras in pyproject.toml. This
# file pins only what is needed to run the test suite and benchmarks reproducibly
# in CI and locally.
#
# Install with:
#   pip install -r requirements-dev.txt
pytest==8.3.4

# Optional at plugin runtime (the host Hermes venv provides it), but the test
# suite reads host config / model-cache YAML to exercise verifier-model option
# discovery, so it must be present in CI for those code paths to run.
PyYAML==6.0.3
